OCaml utility to use rsync on several folders and with cascade sync to an other server

This is an application intended to be used with rsync
allowing you some flexible parametring and multiple folders
synchronisation.
